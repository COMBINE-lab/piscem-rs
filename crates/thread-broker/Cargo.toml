[package]
name = "thread-broker"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license-file = "../../LICENSE"
description = "Closed-loop scheduling of a fixed thread budget between a decompressor and the work that consumes it"
publish = false

[dependencies]
tracing = "0.1"
